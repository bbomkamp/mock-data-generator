export interface MockUser {
    firstName: string;
    lastName: string;
    email: string;
    birthday: string;
}

export interface FieldSelection {
    firstName: boolean;
    lastName: boolean;
    email: boolean;
    birthday: boolean;
}